{
  "name": "odin-catalog-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node bootstrap.mjs",
    "check": "node --check server.mjs && node --check bootstrap.mjs && node --check smoke-test.mjs",
    "test": "node smoke-test.mjs",
    "postinstall": "npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "redis": "^5.0.0"
  }
}
